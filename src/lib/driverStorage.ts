export type { Driver } from "@/lib/supabase/drivers"
export {
  getDrivers,
  getDriverById,
  createDriver,
  createDriver as saveDriver,
  updateDriver,
  deleteDriver,
} from "@/lib/supabase/drivers"
